# CADYomi

## Distribution

Build a Windows installer with `npm run build:desktop`. Generate the Android
project with `npm run android:add`, then build it with `npm run android:build`.
See [distribution.md](docs/distribution.md) for JDK, Android SDK, signing,
and sharing requirements.